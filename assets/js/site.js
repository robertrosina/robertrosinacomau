
const menuButton=document.querySelector('.menu-button');const mobileNav=document.querySelector('.mobile-nav');if(menuButton&&mobileNav){menuButton.addEventListener('click',()=>{const open=mobileNav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));mobileNav.setAttribute('aria-hidden',String(!open))});mobileNav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobileNav.classList.remove('open');menuButton.setAttribute('aria-expanded','false');mobileNav.setAttribute('aria-hidden','true')}));}
const player=document.getElementById('audioPlayer');const title=document.getElementById('audioTitle');const meta=document.getElementById('audioMeta');const tracks=[...document.querySelectorAll('.audio-track')];if(player&&title&&meta&&tracks.length){tracks.forEach(btn=>btn.addEventListener('click',async()=>{const src=btn.dataset.src;if(player.getAttribute('src')!==src){player.pause();player.setAttribute('src',src);player.load()}title.textContent=btn.dataset.title;meta.textContent=btn.dataset.meta;tracks.forEach(x=>{const active=x===btn;x.classList.toggle('active',active);x.setAttribute('aria-pressed',String(active))});try{await player.play()}catch(e){}}));}
